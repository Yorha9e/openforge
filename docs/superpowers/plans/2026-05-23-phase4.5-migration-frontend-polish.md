# Phase 4.5 — 数据库迁移 + 前端补完 实现计划

> **状态: ✅ 已完成**

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 添加数据库迁移自动执行器 + 前端体验补完（骨架屏 / Toast 通知 / 页面过渡），完成 MVP 最后一公里。

**Architecture:** 在 `bootstrap.go` 中注入 MigrationRunner，启动时按序执行 `migrations/` 目录下的 SQL 文件，使用 `schema_migrations` 表跟踪已执行版本。前端创建 `<Skeleton />` 组件族替换所有 "Loading..." 文本，创建 `<Toast />` 通知系统替代 `console.error`，给路由切换加 CSS transition。

**Tech Stack:** Go 1.25 + `database/sql` + `lib/pq`, React 19 + TypeScript, 复用现有 design-tokens

---
## File Map

```
openforge/
├── internal/shared/profile/
│   ├── migrate.go              # NEW: MigrationRunner
│   ├── migrate_test.go         # NEW: migration runner tests
│   └── bootstrap.go            # MODIFY: 启动时调用 migrate
├── migrations/
│   └── (已有 001/003, 无需新增)
└── frontend/src/
    ├── shared/
    │   ├── toast.tsx           # NEW: Toast 通知系统
    │   └── skeleton.tsx        # NEW: Skeleton 骨架屏组件
    ├── features/
    │   ├── dashboard/
    │   │   └── DashboardPage.tsx  # MODIFY: Skeleton + Toast
    │   ├── project/
    │   │   └── ProjectPage.tsx    # MODIFY: Skeleton + Toast
    │   ├── review-inbox/
    │   │   └── ReviewInboxPage.tsx # MODIFY: Skeleton + Toast
    │   ├── login/
    │   │   └── LoginPage.tsx      # MODIFY: Toast on error
    │   ├── chat/
    │   │   ├── MessageList.tsx    # MODIFY: Skeleton + empty state
    │   │   └── ChatPanel.tsx      # MODIFY: Skeleton wrapper
    │   └── code-review/
    │       └── GatePanel.tsx      # MODIFY: Toast on error
    └── App.tsx                    # MODIFY: 页面过渡动画
```

---

### Task 1: MigrationRunner

> 在 `bootstrap.go` 启动时自动执行 `migrations/` 目录下的 `.up.sql` 文件

**Files:**
- Create: `internal/shared/profile/migrate.go`
- Create: `internal/shared/profile/migrate_test.go`
- Modify: `internal/shared/profile/bootstrap.go:80-88`

- [ ] **Step 1: 写 migration runner 测试**

Create `internal/shared/profile/migrate_test.go`:

```go
package profile

import (
	"database/sql"
	"os"
	"path/filepath"
	"testing"
)

func TestMigrationRunner_Run(t *testing.T) {
	// Use a temporary directory as the migrations source
	dir := t.TempDir()
	os.WriteFile(filepath.Join(dir, "001_test.up.sql"), []byte(`
		CREATE TABLE IF NOT EXISTS _test_migration (
			id SERIAL PRIMARY KEY,
			name TEXT NOT NULL
		);
	`), 0644)
	os.WriteFile(filepath.Join(dir, "002_test.up.sql"), []byte(`
		INSERT INTO _test_migration (name) VALUES ('hello');
	`), 0644)

	// Open an in-memory / file-based SQLite-like test DB.
	// For Postgres-dependent tests, use t.Skip if PG not available.
	db, err := sql.Open("postgres", os.Getenv("TEST_DATABASE_URL"))
	if err != nil || os.Getenv("TEST_DATABASE_URL") == "" {
		t.Skip("TEST_DATABASE_URL not set")
	}
	defer db.Close()

	runner := NewMigrationRunner(db, dir)
	if err := runner.Run(t.Context()); err != nil {
		t.Fatalf("Run() failed: %v", err)
	}

	// Verify migrations tracking table exists
	var count int
	db.QueryRow("SELECT COUNT(*) FROM schema_migrations").Scan(&count)
	if count != 2 {
		t.Errorf("expected 2 migrations tracked, got %d", count)
	}

	// Verify idempotent: run again should be no-op
	if err := runner.Run(t.Context()); err != nil {
		t.Fatalf("second Run() failed: %v", err)
	}
	db.QueryRow("SELECT COUNT(*) FROM schema_migrations").Scan(&count)
	if count != 2 {
		t.Errorf("expected still 2 after re-run, got %d", count)
	}
}
```

- [ ] **Step 2: 运行测试 — 失败**

Run: `TEST_DATABASE_URL="host=localhost port=5432 user=openforge password=openforge_dev dbname=openforge sslmode=disable" go test ./internal/shared/profile/ -v -run TestMigrationRunner -count=1`
Expected: FAIL — NewMigrationRunner not defined

- [ ] **Step 3: 实现 MigrationRunner**

Create `internal/shared/profile/migrate.go`:

```go
package profile

import (
	"database/sql"
	"fmt"
	"os"
	"path/filepath"
	"sort"
	"strings"
)

// MigrationRunner executes .up.sql files from a directory in lexicographic order.
// It tracks executed migrations in a schema_migrations table for idempotency.
type MigrationRunner struct {
	db  *sql.DB
	dir string
}

func NewMigrationRunner(db *sql.DB, dir string) *MigrationRunner {
	return &MigrationRunner{db: db, dir: dir}
}

// Run executes all pending .up.sql files, wrapping each in a transaction.
// Already-executed migrations are skipped (tracked by filename in schema_migrations).
func (r *MigrationRunner) Run(ctx context.Context) error {
	if err := r.ensureTrackingTable(ctx); err != nil {
		return fmt.Errorf("migrate: tracking table: %w", err)
	}

	entries, err := os.ReadDir(r.dir)
	if err != nil {
		return fmt.Errorf("migrate: read dir %s: %w", r.dir, err)
	}

	var files []string
	for _, e := range entries {
		if !e.IsDir() && strings.HasSuffix(e.Name(), ".up.sql") {
			files = append(files, e.Name())
		}
	}
	sort.Strings(files)

	for _, f := range files {
		executed, err := r.isExecuted(ctx, f)
		if err != nil {
			return fmt.Errorf("migrate: check %s: %w", f, err)
		}
		if executed {
			continue
		}

		content, err := os.ReadFile(filepath.Join(r.dir, f))
		if err != nil {
			return fmt.Errorf("migrate: read %s: %w", f, err)
		}

		tx, err := r.db.BeginTx(ctx, nil)
		if err != nil {
			return fmt.Errorf("migrate: begin tx for %s: %w", f, err)
		}

		if _, err := tx.ExecContext(ctx, string(content)); err != nil {
			tx.Rollback()
			return fmt.Errorf("migrate: exec %s: %w", f, err)
		}

		if _, err := tx.ExecContext(ctx,
			`INSERT INTO schema_migrations (filename) VALUES ($1)`, f); err != nil {
			tx.Rollback()
			return fmt.Errorf("migrate: track %s: %w", f, err)
		}

		if err := tx.Commit(); err != nil {
			return fmt.Errorf("migrate: commit %s: %w", f, err)
		}
	}
	return nil
}

func (r *MigrationRunner) ensureTrackingTable(ctx context.Context) error {
	_, err := r.db.ExecContext(ctx, `
		CREATE TABLE IF NOT EXISTS schema_migrations (
			filename   VARCHAR(255) PRIMARY KEY,
			executed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
		)
	`)
	return err
}

func (r *MigrationRunner) isExecuted(ctx context.Context, filename string) (bool, error) {
	var exists bool
	err := r.db.QueryRowContext(ctx,
		`SELECT EXISTS(SELECT 1 FROM schema_migrations WHERE filename = $1)`, filename).Scan(&exists)
	return exists, err
}
```

- [ ] **Step 4: 运行测试 — 通过**

Run: `TEST_DATABASE_URL="host=localhost port=5432 user=openforge password=openforge_dev dbname=openforge sslmode=disable" go test ./internal/shared/profile/ -v -run TestMigrationRunner -count=1`
Expected: PASS

- [ ] **Step 5: 集成到 Bootstrap**

In `internal/shared/profile/bootstrap.go`, after `of.DB = db` (line ~88), before `return of, nil`, add:

```go
	// Run database migrations
	runner := NewMigrationRunner(db, "migrations")
	if err := runner.Run(context.Background()); err != nil {
		return nil, fmt.Errorf("migration: %w", err)
	}
```

- [ ] **Step 6: 编译验证 + Commit**

```bash
go build ./cmd/server/
go test ./internal/shared/profile/ -count=1
```

```bash
git add internal/shared/profile/migrate.go internal/shared/profile/migrate_test.go internal/shared/profile/bootstrap.go
git commit -m "feat(db): add migration runner with idempotent schema_migrations tracking"
```

---

### Task 2: Skeleton 骨架屏组件

> 创建可复用的 Skeleton 组件，替换所有 "Loading..." 文本

**Files:**
- Create: `frontend/src/shared/skeleton.tsx`

- [ ] **Step 1: 创建 Skeleton 组件**

Create `frontend/src/shared/skeleton.tsx`:

```tsx
import { tokens } from './design-tokens';

interface SkeletonProps {
  width?: number | string;
  height?: number | string;
  borderRadius?: number;
  style?: React.CSSProperties;
}

/** Single shimmer line/block for loading placeholders. */
export function Skeleton({ width = '100%', height = 16, borderRadius = 4, style }: SkeletonProps) {
  return (
    <div style={{
      width,
      height,
      borderRadius,
      background: tokens.surface,
      animation: 'of-shimmer 1.5s ease-in-out infinite',
      ...style,
    }} />
  );
}

/** Card-sized skeleton matching ProjectCard dimensions. */
export function CardSkeleton() {
  return (
    <div style={{ background: tokens.surface, border: `1px solid ${tokens.border}`, borderRadius: 8, padding: 16 }}>
      <Skeleton width="60%" height={20} />
      <div style={{ height: 8 }} />
      <Skeleton width="80%" height={14} />
      <div style={{ height: 4 }} />
      <Skeleton width="40%" height={12} />
    </div>
  );
}

/** Text block skeleton for message/paragraph content. */
export function TextSkeleton({ lines = 3 }: { lines?: number }) {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
      {Array.from({ length: lines }).map((_, i) => (
        <Skeleton key={i} width={i === lines - 1 ? '60%' : '100%'} height={14} />
      ))}
    </div>
  );
}

/** Full page skeleton: header + grid of cards. */
export function PageSkeleton({ cards = 3 }: { cards?: number }) {
  return (
    <div style={{ minHeight: '100vh', background: tokens.bg }}>
      <div style={{ padding: '12px 24px', borderBottom: `1px solid ${tokens.border}`, display: 'flex', alignItems: 'center', gap: 16 }}>
        <Skeleton width={120} height={20} />
        <div style={{ marginLeft: 'auto' }}>
          <Skeleton width={80} height={14} />
        </div>
      </div>
      <div style={{ maxWidth: 960, margin: '0 auto', padding: 24 }}>
        <Skeleton width={140} height={28} style={{ marginBottom: 24 }} />
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(320px, 1fr))', gap: 16 }}>
          {Array.from({ length: cards }).map((_, i) => <CardSkeleton key={i} />)}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 添加 shimmer 动画到 index.html**

In `frontend/index.html`, add the keyframes before `</style>` or to the existing `<style>` block:

```css
@keyframes of-shimmer {
  0% { opacity: 0.4; }
  50% { opacity: 0.8; }
  100% { opacity: 0.4; }
}
```

- [ ] **Step 3: 替换 DashboardPage 的 loading**

In `DashboardPage.tsx`, import Skeleton:
```tsx
import { PageSkeleton } from '../../shared/skeleton';
```

Replace `{loading ? <p style={{ color: tokens.muted }}>Loading...</p>` with:
```tsx
{loading ? <PageSkeleton /> : ...}
```

- [ ] **Step 4: 替换 ProjectPage + ReviewInboxPage 的 loading**

Same pattern for both pages — replace "Loading..." text with:
```tsx
import { PageSkeleton } from '../../shared/skeleton';
// Replace: loading ? <p>Loading...</p>
// With: loading ? <PageSkeleton cards={1} />
```

- [ ] **Step 5: 替换 ChatPanel + MessageList 的 loading**

ChatPanel uses `if (!pipelineId) return null` — add a skeleton state when loading:
```tsx
import { TextSkeleton } from '../../shared/skeleton';
// In MessageList: when messages undefined/loading, show <TextSkeleton lines={5} />
```

- [ ] **Step 6: 编译验证 + Commit**

```bash
cd frontend && npx tsc --noEmit
```

```bash
git add frontend/src/shared/skeleton.tsx frontend/index.html frontend/src/features/
git commit -m "feat(frontend): add Skeleton shimmer components, replace all Loading... text"
```

---

### Task 3: Toast 通知系统

> 创建全局 Toast 通知，替代 `console.error` 静默吞错

**Files:**
- Create: `frontend/src/shared/toast.tsx`
- Modify: `frontend/src/main.tsx` — 注入 ToastProvider
- Modify: 5 pages — 用 toast 替换 console.error

- [ ] **Step 1: 创建 Toast 组件**

Create `frontend/src/shared/toast.tsx`:

```tsx
import { createContext, useContext, useState, useCallback, type ReactNode } from 'react';
import { tokens } from './design-tokens';

interface ToastItem {
  id: number;
  message: string;
  type: 'error' | 'success' | 'info';
}

interface ToastCtx {
  toast: (message: string, type?: ToastItem['type']) => void;
}

const ToastContext = createContext<ToastCtx>({ toast: () => {} });

let nextId = 0;

export function ToastProvider({ children }: { children: ReactNode }) {
  const [items, setItems] = useState<ToastItem[]>([]);

  const toast = useCallback((message: string, type: ToastItem['type'] = 'error') => {
    const id = nextId++;
    setItems(prev => [...prev, { id, message, type }]);
    setTimeout(() => setItems(prev => prev.filter(i => i.id !== id)), 4000);
  }, []);

  const bgMap = { error: tokens.error, success: tokens.cta, info: tokens.surface };
  const textMap = { error: '#fff', success: tokens.ctaText, info: tokens.text };

  return (
    <ToastContext.Provider value={{ toast }}>
      {children}
      <div style={{ position: 'fixed', top: 16, right: 16, zIndex: 9999, display: 'flex', flexDirection: 'column', gap: 8 }}>
        {items.map(item => (
          <div key={item.id} style={{
            background: bgMap[item.type], color: textMap[item.type],
            padding: '10px 16px', borderRadius: 6, fontSize: 14, fontWeight: 500,
            fontFamily: tokens.fontBody, maxWidth: 360, boxShadow: '0 4px 12px rgba(0,0,0,0.4)',
            animation: 'of-slide-in 200ms ease-out',
          }}>
            {item.message}
          </div>
        ))}
      </div>
    </ToastContext.Provider>
  );
}

export function useToast() { return useContext(ToastContext); }
```

- [ ] **Step 2: 添加 slide-in 动画到 index.html**

```css
@keyframes of-slide-in {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}
```

- [ ] **Step 3: 注入 ToastProvider**

In `main.tsx`, wrap with ToastProvider:

```tsx
import { ToastProvider } from './shared/toast';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter>
      <AuthProvider>
        <ToastProvider>
          <App />
        </ToastProvider>
      </AuthProvider>
    </BrowserRouter>
  </StrictMode>
);
```

- [ ] **Step 4: 替换各页面的 console.error → toast**

In each page that has `catch(console.error)` or silent error handling:
- **DashboardPage**: `catch(err => setError(...))` — already fixed, keep as is
- **LoginPage**: `catch(err => setError(...))` — keep, + toast
- **ProjectPage**: replace silent `catch(err) { console.error(err) }` with `toast(err.message)`
- **GatePanel**: replace silent catch with `setSubmitError(...)` + `toast(err.message)`
- **ReviewInboxPage**: add toast on API failure

```tsx
import { useToast } from '../../shared/toast';
const { toast } = useToast();
// In catch: toast(err instanceof Error ? err.message : 'Request failed');
```

- [ ] **Step 5: 编译验证 + Commit**

```bash
cd frontend && npx tsc --noEmit && npm run build
```

```bash
git add frontend/src/shared/toast.tsx frontend/src/main.tsx frontend/index.html
git commit -m "feat(frontend): add Toast notification system, replace silent console.error"
```

---

### Task 4: 页面过渡动画

> 给路由切换添加 fade 过渡效果

**Files:**
- Modify: `frontend/src/App.tsx`
- Create: `frontend/src/shared/page-transition.css`

- [ ] **Step 1: 添加页面容器过渡样式**

Create `frontend/src/shared/page-transition.css`:

```css
.page-enter {
  animation: of-fade-in 200ms ease-out both;
}

@keyframes of-fade-in {
  from { opacity: 0; transform: translateY(4px); }
  to { opacity: 1; transform: translateY(0); }
}
```

- [ ] **Step 2: 包装路由组件**

Modify `App.tsx` to use React `key` on routes to trigger re-mount + animate:

Add this import:
```tsx
import './shared/page-transition.css';
```

Wrap ProtectedRoute children in a div with animation class:

```tsx
function PageWrapper({ children }: { children: React.ReactNode }) {
  return <div className="page-enter">{children}</div>;
}

function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { token } = useAuth();
  if (!token) return <Navigate to="/login" replace />;
  return <PageWrapper>{children}</PageWrapper>;
}
```

- [ ] **Step 3: 编译验证 + Commit**

```bash
cd frontend && npx tsc --noEmit && npm run build
```

```bash
git add frontend/src/shared/page-transition.css frontend/src/App.tsx
git commit -m "feat(frontend): add fade-in page transition animation on route change"
```

---

### Task 5: E2E 验证

- [ ] **Step 1: Go 测试**

```bash
go test ./internal/shared/profile/ -count=1
```
Expected: PASS (migration test skips without PG)

- [ ] **Step 2: Go build**

```bash
go build ./cmd/server/
```
Expected: 编译通过

- [ ] **Step 3: 前端编译 + 构建**

```bash
cd frontend && npx tsc --noEmit && npm run build
```
Expected: 零错误

- [ ] **Step 4: 启动全栈验证**

```bash
# Terminal 1: Go (需 PostgreSQL 运行)
$env:ANTHROPIC_AUTH_TOKEN = "sk-test"
go run ./cmd/server/ --addr :8030

# Terminal 2: 前端
cd frontend && npm run dev

# 验证:
# 1. 访问 http://localhost:5173 登录
# 2. 首次启动应自动执行迁移，日志显示 migration runner 执行
# 3. 刷新 Dashboard 应看到 skeleton 闪烁而非 "Loading..."
# 4. 制造错误 (如杀 DB) 应看到 toast 弹窗而非静默失败
# 5. 页面切换应有 200ms fade 效果
```

- [ ] **Step 5: Commit**

```bash
git add internal/ frontend/ go.mod go.sum
git commit -m "chore(phase4.5): final verification — migrations run, skeleton+toast+transition live"
```

---

## Phase 4.5 Acceptance Criteria

| # | Criterion | Verification |
|---|-----------|-------------|
| 1 | `go build ./cmd/server/` 编译通过 | automated |
| 2 | `go test ./...` 全部 PASS | automated |
| 3 | 启动时自动执行 `migrations/*.up.sql` | DB 日志 |
| 4 | `schema_migrations` 表正确跟踪已执行迁移 | DB 查询 |
| 5 | 重复启动不重复执行迁移 (幂等) | DB 查询 |
| 6 | Dashboard/Project/ReviewInbox 使用 Skeleton 替代 "Loading..." | visual |
| 7 | Toast 通知在 API 错误时弹出 (右上角, 4s 自动消失) | visual |
| 8 | 页面切换有 200ms fade-in 过渡 | visual |
| 9 | `npx tsc --noEmit` 零错误 | automated |
| 10 | `npm run build` 成功 | automated |
